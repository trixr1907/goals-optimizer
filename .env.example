# Base path for the Next.js app router.
# Set to '' (empty string) for root-domain deployments (e.g. goals.example.com).
# Set to '/goals' when deploying under a sub-path (e.g. example.com/goals).
# The default in next.config.js is '/goals' when this variable is not set.
NEXT_PUBLIC_BASE_PATH=
